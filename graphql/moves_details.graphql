query MovesDetails($id: Int!) {
    moveById(moveId: $id) {
        name
        effectEntries {
            effect
        }
        power
        effectChance
        accuracy
        pp
        type {
            name
        }
    }
}
